//! Windows 平台专属胶水占位模块（T0 骨架）。
//!
//! 注意：当前桌面端 Windows 专属代码仍以 `src-tauri/src` 顶层 `mod`
//! （`screenshot` / `smtc` / `dcomp_overlay` / `transfer` / `device` / `services` 等）形式存在，
//! 并未迁入此模块。本模块仅作为 T0 cfg 隔离骨架的一部分占位，预留给后续
//! 可能的 Windows 专属入口封装；对现有 Windows 行为零影响。
